interface ContractsData {
  [ contracts : string ] : any;
}

interface ContractData{
	franchise? 				: number;
	id? 							: number;
	insuranceCompany? : number;
	premium? 					: number;
	type? 						: string;
	vehicle? 					: number;
}